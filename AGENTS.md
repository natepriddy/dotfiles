# AGENTS.md

AI agent guidance for this repo lives in **[CLAUDE.md](CLAUDE.md)**.

It covers the chezmoi two-stage rendering model, trust tier semantics,
safe-staging rules, the test suite (`scripts/local-validate.sh`), and the
repo-specific gotchas that cause silent failures in AI-assisted editing.

If you are not Claude Code, read `CLAUDE.md` — the conventions apply equally.
